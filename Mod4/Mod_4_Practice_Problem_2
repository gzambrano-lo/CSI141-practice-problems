# The headlights of a car should only automatically turn on
# when the daylight outside is below a certain threshold. If a
# sensor threshold is below the number 8, 
# print "Headlights On"; otherwise, print "Headlights Off".'''

# User is prompted to enter the daylight sensor value 
sensor_threshold = int(input("Enter daylight sensor value: ")) 
# If value is less than 8, the program prints "Headlights On" 
if sensor_threshold < 8: 
    print("Headlights On")
# Otherwise, it prints "Headlights Off" 
else: 
    print("Headlights Off")
